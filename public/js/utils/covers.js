// public/js/utils/covers.js
// Minimal stub så appen kör. Vi kan uppgradera i Fas 2.

export async function loadCoversForBooks(books) {
  // Plan (Fas 2): hämta omslag via API + cachea resultat i Map/LocalStorage.
  // Just nu gör vi inget – vi returnerar bara listan.
  return books;
}
